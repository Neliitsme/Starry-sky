{
  "name": "starry-sky",
  "version": "0.0.1",
  "description": "An app for generating a starry sky for your readme file, just because numbers are not enough.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Nelite",
  "license": "ISC",
  "dependencies": {
    "canvas": "^2.9.3",
    "two.js": "^0.8.10"
  },
  "devDependencies": {
    "@vercel/node": "^2.5.8",
    "prettier": "2.7.1",
    "typescript": "^4.7.4"
  }
}
